---
import {
  Card,
  CardContent,
  CardFooter,
  CardHeader,
  CardTitle,
} from '@/components/ui/card.tsx'
import type { CollectionEntry } from 'astro:content'
import { ArrowRight } from 'lucide-react'

interface Props {
  posts: CollectionEntry<'phenomena'>[]
}

const { posts } = Astro.props
---

<section class="prose-ul:-ml-6 prose-ul:list-none">
  <ul
    class="grid auto-rows-fr grid-cols-1 gap-6 md:grid-cols-2 md:items-stretch"
  >
    {
      posts.map((post: any) => {
        return (
          <li>
            <a href={`/phenomena/${post.id}/`}>
              <Card className="h-full flex flex-col ">
                <CardHeader>
                  <CardTitle className="text-xl font-bold">
                    {post.data.title}
                  </CardTitle>
                </CardHeader>
                <CardContent className="flex-1">
                  {post.data.description && (
                    <div class="opacity-70">{post.data.description}</div>
                  )}
                </CardContent>
                <CardFooter className="mb-2">
                  <p class="date flex items-center gap-2 font-mono text-sm opacity-70">
                    {/* <FormattedDate date={post.data.pubDate} /> */}
                    Learn more
                    <ArrowRight className="inline-block size-4" />
                  </p>
                </CardFooter>
              </Card>
            </a>
          </li>
        )
      })
    }
  </ul>
</section>
